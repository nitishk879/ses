<x-dashboard>
    @php($notifications = Auth::user()->notifications)
    <div class="flex flex-col gap-7"><div><p class="m-0 text-sm font-medium text-sky-600">Communication</p><h1 class="mb-0 mt-1 text-3xl font-bold tracking-tight">Messages</h1><p class="mb-0 mt-2 text-sm text-slate-500">Notifications and recent activity from your hiring workspace.</p></div><section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 p-5"><h2 class="m-0 text-lg font-semibold">Inbox</h2><p class="mb-0 mt-1 text-sm text-slate-500">{{ Auth::user()->unreadNotifications->count() }} unread notifications</p></div><div class="divide-y divide-slate-100">@forelse($notifications as $notification)<a href="{{ $notification->data['url'] ?? '#' }}" class="flex gap-4 p-5 no-underline transition hover:bg-slate-50"><span class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-sky-100 text-sky-700">◌</span><span class="min-w-0 flex-1"><span class="block text-sm font-semibold text-slate-800">{{ $notification->data['title'] ?? 'Notification' }}</span><span class="mt-1 block text-sm text-slate-500">{{ $notification->data['message'] ?? '' }}</span><span class="mt-2 block text-xs text-slate-400">{{ $notification->created_at?->diffForHumans() }}</span></span>@if(is_null($notification->read_at))<span class="mt-2 h-2 w-2 shrink-0 rounded-full bg-sky-500"></span>@endif</a>@empty<div class="p-12 text-center"><p class="m-0 text-lg font-semibold">Your inbox is empty</p><p class="mb-0 mt-2 text-sm text-slate-500">New hiring activity and messages will appear here.</p></div>@endforelse</div></section></div>
</x-dashboard>
